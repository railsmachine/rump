#!/usr/bin/env ruby 

begin 
  require 'rump'
rescue LoadError
  $: << File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'rump'
end

command   = ARGV.first
arguments = ARGV[1..-1]

Rump.new.invoke(command, arguments)
